# Do not modify!
# This file was generated by https://github.com/JLarky/gha-ts

name: Check gha-ts sync script
"on":
  push:
    branches:
      - main
  pull_request: {}
jobs:
  checkTypeScript:
    name: Check TypeScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Install dependencies
        run: |
          cd .github/workflows && npm ci
      - name: Check TypeScript
        run: |
          cd .github/workflows && npm run type-check
  checkGhaTsWorkflowsConverted:
    name: Check gha-ts workflows converted
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-node@dda4788290998366da86b6a4f497909644397bb2
        with:
          node-version: "22"
      - name: Install production dependencies
        run: |
          cd .github/workflows && npm ci --omit=dev
      - name: Clear generated workflows
        run: |
          rm -f .github/workflows/*.generated.yml
      - name: Generate TS workflows to yaml
        run: |
          .github/workflows/utils/build-cli.ts
      - name: Verify if TS workflows are converted
        uses: actions/github-script@v8
        with:
          script: |
            return await (
              async ({ exec, core }) => {
              const fs = await import("node:fs");

              // Get changed files (includes both modified tracked files and untracked files)
              let changedFiles = "";
              await exec.exec("git", ["status", "--porcelain"], {
                listeners: {
                  stdout: (data) => {
                    changedFiles += data.toString();
                  },
                },
              });

              // Parse git status output to extract filenames
              // Format: "XY filename" where XY is status code (e.g., " M", "??")
              const files = changedFiles
                .trim()
                .split("\n")
                .filter((line) => line.length > 0)
                .map((line) => line.substring(3).trim()) // Remove status code and space
                .filter((f) => f.length > 0);

              if (files.length > 0) {
                // Create error annotation
                core.error(
                  "Run 'mise run wf-build' locally, commit, and push.",
                  {
                    title: "TS workflows are not up to date",
                  }
                );

                // Group changed files
                core.startGroup("Changed files");
                console.log(files.join("\n"));
                core.endGroup();

                // Create notice for each file
                for (const file of files) {
                  core.notice("Update generated YAML for this file", {
                    file: file,
                    title: "Changed file",
                  });
                }

                // Write to step summary
                const fileList = files.map((f) => "- " + f).join("\n");
                const summary = [
                  "### TS workflows are not up to date",
                  "",
                  "Run: mise run wf-build",
                  "",
                  "Then commit the updated files and push.",
                  "",
                  "Changed files:",
                  "",
                  fileList,
                ].join("\n");

                fs.appendFileSync(
                  process.env.GITHUB_STEP_SUMMARY ,
                  summary + "\n"
                );

                // Exit with error
                core.setFailed("TS workflows are not up to date");
              }

              return "";
            })({ context, core, github, octokit, exec, glob, io, require });
